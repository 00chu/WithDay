    spring.application.name=WithDayBack

    server.port=10400

    # 1. Database connection settings (AWS RDS MySQL)
    # Replace 'your_DB_name' with the Database(Schema) name created in MySQL Workbench.
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
    spring.datasource.url=jdbc:mysql://database-1.c1oiiyo4ewes.ap-northeast-2.rds.amazonaws.com:3306/withday_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    spring.datasource.username=admin
    spring.datasource.password=shin1656

    # 2. MyBatis settings
    # Location of XML mapper files
    # Place .xml mapper files inside src/main/resources/mappers
    mybatis.mapper-locations=classpath:mappers/**/*.xml

    # Package path containing DTO classes
    # Allows using only class names in resultType within XML files
    mybatis.type-aliases-package=com.test.withdayback

    # Automatically maps underscore-style DB column names
    # to camelCase Java variable names
    mybatis.configuration.map-underscore-to-camel-case=true

    mybatis.configuration.jdbc-type-for-null=NULL

    # Print executed SQL queries in the console log (useful during development)
    logging.level.com.test.withdayback.mapper=debug

    # Google Mail settings
    spring.mail.host=smtp.gmail.com
    spring.mail.port=587
    spring.mail.username=shinjiwoong1656@gmail.com
    spring.mail.password=wrfqoqbxdddytsmk
    spring.mail.properties.mail.smtp.auth=true
    spring.mail.properties.mail.smtp.starttls.enable=true

    # Cloudinary settings
    cloudinary.cloud-name=dgg37xl5m
    cloudinary.api-key=185643916866631
    cloudinary.api-secret=Nyh67hOnFMiVxjAQSluUurH8H_s

# 파일 업로드 용량 제한 (기본값이 작아서 설정을 권장합니다)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

onesignal.app-id=${ONESIGNAL_APP_ID}
onesignal.api-key=${ONESIGNAL_API_KEY}