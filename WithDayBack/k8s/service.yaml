apiVersion: v1
kind: Service
metadata:
  name: withday-backend-service
  namespace: withday
spec:
  type: LoadBalancer
  selector:
    app: withday-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 10400
